# Hub Système 2.0 Implementation Plan

> **For agentic workers:** Execute inline in this session (user: « go pour tout »). Tests pytest sans Konsole/pkexec.

**Goal:** Livrer 2.0.0 : console paquets in-app, cliché avant upgrade, score santé, AppStream Flathub + tar.gz public.

**Architecture:** Scripts bash paquets inchangés. Nouveau runner `jobs.spawn` + fenêtre GTK. Santé = fonction pure sur metrics déjà collectées. Snapshot = `backup.create_snapshot` avant apply.

**Tech Stack:** Python 3.10+, GTK4, Libadwaita, pytest, Flatpak GNOME 49.

## Global Constraints

- Flatpak only ; pas de tag Gest sur `linux-releases`.
- Pas de `subprocess(..., shell=True)`.
- Scripts jobs uniquement sous `updater.updates_dir()`.
- VERSION 2.0.0 + ship `.flatpak` + `LEGAL.md` + tar.gz source sur `linux-flatpak-releases`.
- CT README public : dernier tag **Binaire** (pas Mail, pas 1.2.9 forcé).

---

### Task 1: health + jobs (tests d’abord)

**Files:**
- Create: `core/health.py`, `core/jobs.py`, `tests/test_health.py`, `tests/test_jobs.py`
- Modify: `core/host.py` (`popen`)

See implementation in repo after this task. Tests must pass without GTK.

### Task 2: job console + wire packages + snapshot + dashboard

**Files:**
- Create: `ui/job_console.py`
- Modify: `ui/main_window.py`, `ui/pages/dashboard.py`, `core/i18n.py`, `ui/components.py` (css)

### Task 3: docs + 2.0.0 ship

**Files:** VERSION, README, metainfo, release.yml, FLATHUB.md, packaging COMPAT
